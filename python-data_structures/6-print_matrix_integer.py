#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for value in matrix:
        if not value:
            print()
        else:
            for i in row:
                if num == row[-1]:
                    print('{:d}'.format(num))
                else:
                    print('{:d}'.format(num), end=" ")
